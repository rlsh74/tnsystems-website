# Environment Configuration for TN Systems Website

# Server Configuration
NODE_ENV=development
PORT=3000
FRONTEND_URL=https://tnsystems.ai

# Email Configuration (Production)
EMAIL_SERVICE=gmail
EMAIL_USER=tnsystems.ai@gmail.com
EMAIL_PASS=your-gmail-app-password
COMPANY_EMAIL=tnsystems.ai@gmail.com

# Alternative SMTP Configuration (if not using Gmail)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Security
# SESSION_SECRET=your-session-secret-key

# Analytics (Optional)
# GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Database (Future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/tn_systems 